spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: "file:///Users/lizhuolun/Downloads/microservices-demo (10)/config-server-repository"
          clone-on-start: true
    fail-fast: true